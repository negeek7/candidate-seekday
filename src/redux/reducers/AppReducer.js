const initialState = {
    jobDescriptionModalState: false,
}

export function AppReducer(state = initialState, action) {

    switch(action.type){


        default:
            return state
    }

}